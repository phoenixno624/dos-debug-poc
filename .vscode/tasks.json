{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with Open Watcom",
            "type": "shell",
            "command": "wmake",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}